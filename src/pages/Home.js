import React from "react";
import "./home.css"
import Start from "../sections/Start";

const Home = () => {
    return (
        <main>
            <Start />
        </main>
    );
}

export default Home;
